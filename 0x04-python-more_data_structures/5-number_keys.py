#!/usr/bin/python3


def number_keys(a_dictionary):
    """how many keys are in the dictionary

    Args:
        a_dictionary: dictionary to check for keys
    """


    key_count = (len(a_dictionary.keys()))

    return key_count
