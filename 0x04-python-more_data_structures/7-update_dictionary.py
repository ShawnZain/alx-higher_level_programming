#!/usr/bin/python3


def update_dictionary(a_dictionary, key, value):
    """changes the value of a key-value pair if it exists
    adds the new key-value pair if it does not exist

    Args:
        a_dictionary: dict to alter
        key: key to look for
        value: value to change or add
    """


    a_dictionary[key] = value
